# 民法典宣讲答题系统 · 功能清单

> 部署于云服务器，供民法典宣讲现场使用：手机扫码答题，大屏实时看战况，最后公布前三名。

## 一、系统概述

| 项 | 说明 |
|---|---|
| 定位 | 扫码答题 + 实时大屏展示 |
| 部署 | 云服务器（Node.js + Express + SQLite），手机走公网，无需现场局域网 |
| 题库 | 14 个板块、36 题（7 大民生亮点 + 七大编），源自民法典宣讲文字稿 |
| 实时机制 | 展示页每 1 秒自动拉取最新状态 |

## 二、角色与页面（4 个）

| 页面 | 使用者 | 作用 |
|---|---|---|
| 管理台 `/` | 主持人（电脑） | 生成/下载二维码、切换展示页 |
| 答题页 `/answer?section=id` | 群众（手机扫码） | 填昵称答题 |
| 分区展示页 `/display?section=id` | 大屏 | 单板块实时答题情况 |
| 最终总览页 `/final` | 大屏 | 前三名颁奖台 + 实时总排名 |

## 三、功能清单

### 1. 管理台

- [x] 列出全部 14 个板块（标题、副标题、题数）
- [x] 每板块自动生成二维码（指向该板块答题页）
- [x] 二维码**下载 PNG**（图片下方带板块名称标题，方便贴 PPT）
- [x] 复制答题链接
- [x] 一键打开该板块「实时展示页」（新标签页）
- [x] 一键打开「最终总览页」（新标签页）
- [x] 顶部实时概况（参与人数 / 已答题目 / 答对题目）
- [x] 公网地址自适应（`PUBLIC_URL` 环境变量优先，否则用当前访问地址）

### 2. 答题页（移动端）

- [x] 按 URL 参数加载对应板块题目
- [x] 扫码后本地**无昵称（token）**时，先显示"昵称门"提示填写
- [x] 昵称门提交前**数据库唯一性查重**：昵称已被他人占用则提示更换，不进入答题
- [x] **填完昵称后才显示题目、允许答题**（未填昵称看不到题），昵称填后不可修改
- [x] 昵称保存本地（localStorage），下次扫码免填直接答题
- [x] 判断题 / 单选题，大按钮、移动端自适应
- [x] **全部题目答完才能提交**：进度提示"已答 X/N 题" + 按钮置灰
- [x] 提交按钮置灰 + **转圈加载动画**（提交中…）
- [x] 提交后展示**逐题答题报告**：每题对错 + 正确答案（不含解析）
- [x] 报告顶部显示**当前用户累计统计**：累计答题数、答对题数（含正确率）、参与板块数
- [x] 同一板块**限提交一次**（重复提交被拦截并提示），再次扫码直接显示已提交的结果
- [x] 设备标识（localStorage 生成）用于同名防撞
- [x] 同一昵称可跨板块继续作答，成绩跨板块累加

### 3. 分区实时展示页（双栏同屏布局）

- [x] 板块标题 / 副标题 + 总览统计（**参与人数 / 总体正确率**）
- [x] **左栏·各题答题情况**：每题回答人数、正确率、**每个选项的选择人数与占比**（选项条，正确选项绿色高亮）、✅正确答案
- [x] **右栏·实时动态排名**：按当前板块**正确率降序 + 答题时间早者优先**，前 3 名金银铜高亮
- [x] 每 1 秒自动刷新 + "实时更新中"呼吸灯 + 接口禁用缓存（`no-store`）

### 4. 最终总览页（固定屏幕布局，前三名 + 总排名融合）

- [x] 顶部固定：参与人数 / 答题总数 / 总体正确率
- [x] **前三名荣誉**：颁奖台梯度排布（左第二 → 中第一 → 右第三，台阶高低错落）固定展示
- [x] **实时总排名表**（排名 / 昵称 / 答对题数 / 答题总数 / 正确率 / 参与板块），区域内部滚动
- [x] 整页固定屏幕长宽（不整页滚动），每 1 秒自动刷新
- [x] 排名唯一递增（答对题数降序，同分按提交时间早者优先）

### 5. 后端接口

| 方法 | 路径 | 说明 | 鉴权 |
|---|---|---|---|
| GET | /api/meta | 公网地址 | 无 |
| GET | /api/sections | 板块列表 | 无 |
| GET | /api/questions?section= | 题目（不含答案） | 无 |
| GET | /api/state | 实时状态 | 无 |
| GET | /api/qr?t= | 生成二维码 | 无 |
| GET | /api/my-result | 查询用户某板块答题结果 | 无 |
| GET | /api/check-nickname | 昵称查重 | 无 |
| POST | /api/submit | 提交答案 | 无 |

### 6. 数据库（SQLite，data/quiz.db）

| 表 | 说明 |
|---|---|
| `answers` | 答题记录（昵称、板块、答案、答对数、时间） |
| `owners` | 昵称归属（同名防撞：同一昵称只能同一设备） |

- 启动自动建库建表，重启数据不丢
- 免安装数据库服务，备份 = 复制 `data/quiz.db`

## 四、业务规则（已确认）

| 项 | 规则 |
|---|---|
| 板块划分 | 14 个大类（7 亮点 + 7 编），各一个二维码 |
| 排名 | 答对题数降序；同分按提交时间早者优先；名次唯一递增（1,2,3…） |
| 答案显示 | 提交后展示逐题对错 + 正确答案（不含解析） |
| 身份 | 只填昵称 |
| 同名防撞 | 同一昵称只能由同一设备使用 |
| 重复提交 | 同板块限提交一次（提交后锁定），跨板块成绩累加 |
| 荣誉称号 | 学法达人 / 普法之星 / 用法先锋 |
| 前三名展示 | 融合在总览页颁奖台，实时展示 |
| 二维码样式 | 可下载 PNG，带板块名称标题 |

## 五、安全机制

- **XSS 防护**：昵称后端清洗（去除 HTML 危险字符）+ 前端 HTML 转义（双重防护）
- **同名昵称防撞**：服务器签发 httpOnly Cookie 作设备标识（清 localStorage 也不丢、难以伪造），同一昵称只能同一设备使用
- 同板块限提交一次（服务端强制）
- 系统无重置功能、无管理密码；如需清空数据：停止服务后删除 `data/quiz.db`（下次启动自动重建空库）

## 六、部署运行

- 环境变量：`PORT`（默认 3000）、`PUBLIC_URL`（公网地址）
- 推荐 PM2 守护进程，Nginx + HTTPS 可选
- 详见 `DEPLOY.md`
